<div class="divider {{ componentVariation }}" data-uri="{{ default _ref _self }}">
  {{!-- Check that we're dealing with the base variation --}}
  {{#if (compare componentVariation "===" "divider")}}
    {{!-- {{#ifAny title @root.locals.edit}}
    
    {{/ifAny}} --}}
  {{/if}}
</div>
